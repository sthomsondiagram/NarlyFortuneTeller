AI_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Printer Configuration
# Primary: Manufacture_Virtual_PRN (standard for this installation)
PRINTER_NAME=Manufacture_Virtual_PRN

# Optional: Direct USB ESC/POS (leave commented unless using direct USB)
# If USB is configured but fails, will automatically fall back to PRINTER_NAME
# ESCPOS_USB_VENDOR_ID=0x0416
# ESCPOS_USB_PRODUCT_ID=0x5011
# ESCPOS_IN_EP=0x82
# ESCPOS_OUT_EP=0x01
